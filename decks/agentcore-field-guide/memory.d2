direction: right

# ── Agent ────────────────────────────────────────────────────────────────────

agent: "Agent" {
  shape: person
}

# ── Memory ───────────────────────────────────────────────────────────────────

mem: "Memory" {
  style.stroke: "#00A4A6"
  style.fill: "#E6F7F7"
  style.border-radius: 8
}

# ── Storage Types ────────────────────────────────────────────────────────────

short_term: "Short-Term" {
  style.stroke: "#FF9900"
  style.fill: "#FFF8F0"
  style.border-radius: 8

  session: "Conversation history"
  events: "Raw events"
}

long_term: "Long-Term" {
  style.stroke: "#3F8624"
  style.fill: "#F0FFF0"
  style.border-radius: 8

  prefs: "User preferences"
  facts: "Facts & semantic"
  summaries: "Session summaries"
  episodic: "Episodic"
}

# ── Encryption ───────────────────────────────────────────────────────────────

kms: "KMS" {
  shape: image
  icon: ../aws-icons/security-identity/aws-key-management-service.svg
}

# ── Connections ──────────────────────────────────────────────────────────────

agent <-> mem: "read / write"
mem -> short_term: "per session"
mem -> long_term: "async extraction"
short_term -> long_term: "async" {
  style.stroke-dash: 4
}
mem -> kms: "encrypt / decrypt" {
  style.stroke-dash: 4
}
